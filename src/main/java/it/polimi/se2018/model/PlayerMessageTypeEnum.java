package it.polimi.se2018.model;

/**
 * Enum for parsing of PlayerMessage class
 */
public enum PlayerMessageTypeEnum {
    CHECK_MOVE, APPLY_CHOICE, CHOICE, USER, UPDATE, YOUR_TURN, ERROR, MESSAGE, DISCONNECTED, WINNER, DEFAULTCHOICE, EXIT, PING
}